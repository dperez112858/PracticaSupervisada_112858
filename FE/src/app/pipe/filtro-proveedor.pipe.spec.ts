import { FiltroProveedorPipe } from './filtro-proveedor.pipe';

describe('FiltroProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
