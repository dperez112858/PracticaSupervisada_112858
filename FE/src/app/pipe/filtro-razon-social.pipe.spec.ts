import { FiltroRazonSocialPipe } from './filtro-razon-social.pipe';

describe('FiltroRazonSocialPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRazonSocialPipe();
    expect(pipe).toBeTruthy();
  });
});
