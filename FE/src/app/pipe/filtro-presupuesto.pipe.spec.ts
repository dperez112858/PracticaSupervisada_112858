import { FiltroPresupuestoPipe } from './filtro-presupuesto.pipe';

describe('FiltroPresupuestoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPresupuestoPipe();
    expect(pipe).toBeTruthy();
  });
});
