import { FiltroInsumoPipe } from './filtro-insumo.pipe';

describe('FiltroInsumoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroInsumoPipe();
    expect(pipe).toBeTruthy();
  });
});
